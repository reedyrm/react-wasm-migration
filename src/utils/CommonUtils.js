


export const hello = (name) => {

  return `Hello, ${name}`;
};

export const add = (num1, num2) => {
  
  return (+num1) + (+num2);
};

export const sub = (num1, num2) => {
  
  return num1 - num2;
};
